#include <string.h>

/**
 * _strcpy - Copy a string from src to dest
 * @src: String source to be copied to dest
 * @dest: Destination where to paste src
 * Return: Pointer to dest string
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
