#include <string.h>

/**
 * _strlen - Takes a string param and return its length
 * @s: The string who's length must be returned
 * Return: Int, length of the string s
 */
int _strlen(char *s)
{
	return (strlen(s));
}
