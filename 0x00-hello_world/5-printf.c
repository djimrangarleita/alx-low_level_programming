#include<stdio.h>

/**
 * main - Entry point
 * use printf to print a string on stdo
 * return 0 on success
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
